get the array of colorschemes from vim.fn.getcompletion('', 'color')
separate each item into a string with separate lines for each
add function that will switch colorscheme to the current line hovered

TODO:
make things more customizable with an options file, and setup function
create a preview window, with window specific colorscheme and customizable preview message
    note: https://github.com/neovim/neovim/pull/6597, https://github.com/neovim/neovim/issues/1457
    find way to list key-value pairs of all highlight groups of each colorscheme, and apply into above (winhighlight)
